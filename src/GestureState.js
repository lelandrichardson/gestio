const POSSIBLE = 0;
const BEGAN = 1;
const CHANGED = 2;
const RECOGNIZED = 3;
const ENDED = RECOGNIZED;
const FAILED = 4;
const CANCELED = 5;

module.exports = {
  POSSIBLE,
  BEGAN,
  CHANGED,
  RECOGNIZED,
  ENDED,
  FAILED,
  CANCELED,
};
